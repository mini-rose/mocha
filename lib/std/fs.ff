/* fs.ff - standard filesystem operations
   Copyright (c) 2023 mini-rose */

__builtin_decl_mangled("mkdir", null, &str, i32)
fn mkdir(path: str, mode: i32) {
	mkdir(&path, mode)
}

__builtin_decl_mangled("touch", null, &str, i32)
fn touch(path: str, mode: i32) {
	touch(&path, mode)
}

__builtin_decl_mangled("is_file", bool, &str)
fn is_file(path: str) -> bool {
	ret is_file(&path)
}

__builtin_decl_mangled("is_dir", bool, &str)
fn is_dir(path: str) -> bool {
	ret is_dir(&path)
}
