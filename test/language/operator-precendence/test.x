use std.io

fn main {
	x = 5 + 5 * 2 * (2 + 2 * 2)
	io.write(str(x))
    io.write('\n')
	y = 5 + 5 * 1 + ((6 * 7) + 1 * 55)
	io.write(str(y))
    io.write('\n')
	a = 100 / 5 + 3 * 2 - 4
	io.write(str(a))
    io.write('\n')
	b = 50 % 7 + 6 * (3 + 2)
	io.write(str(b))
    io.write('\n')
	z = 5 + 5 * 5 * 5 * 5 + 2 * 2 * 2
	io.write(str(z))
    io.write('\n')
}
