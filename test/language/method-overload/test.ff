use std.io

type T {
	f: fn (self: &T, string: str) {
		print(&string)
	}

	f: fn (self: &T, num: i32) {
		print(num)
	}

	f: static fn (string: str) {
		print(&string)
	}

	f: static fn (num: i32) {
		print(num)
	}
}

fn main {
	t: T
	t.f('hello')
	t.f(123)
	T.f('hello')
	T.f(123)
}
